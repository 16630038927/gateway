server:
  port: 9001
spring:
  application:
    name: product

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true


